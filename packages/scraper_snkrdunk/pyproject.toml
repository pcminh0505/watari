[project]
name = "pokeprice-snkrdunk"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pokeprice-core",
    "httpx>=0.27",
]

[tool.uv.sources]
pokeprice-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pokeprice_snkrdunk"]
