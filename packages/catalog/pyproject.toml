[project]
name = "pokeprice-catalog"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pokeprice-core",
    "httpx>=0.27",
    "curl-cffi>=0.7",
    "beautifulsoup4>=4.12",
    "lxml>=5.0",
    "pyyaml>=6.0",
    "ruamel-yaml>=0.18",
]

[tool.uv.sources]
pokeprice-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pokeprice_catalog"]
